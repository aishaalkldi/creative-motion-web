/** Minimum interactive height for primary patient portal controls (senior-friendly). */
export const PATIENT_PRIMARY_TOUCH_MIN_CLASS = "min-h-[48px]";

/** Square icon control matching the 48px minimum touch target. */
export const PATIENT_ICON_TOUCH_TARGET_CLASS =
  "inline-flex h-12 w-12 min-h-[48px] min-w-[48px] items-center justify-center";
